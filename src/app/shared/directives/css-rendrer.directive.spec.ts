import { CssRendrerDirective } from './css-rendrer.directive';

describe('CssRendrerDirective', () => {
  it('should create an instance', () => {
    const directive = new CssRendrerDirective();
    expect(directive).toBeTruthy();
  });
});
