import { CssBasicsDirective } from './css-basics.directive';

describe('CssBasicsDirective', () => {
  it('should create an instance', () => {
    const directive = new CssBasicsDirective();
    expect(directive).toBeTruthy();
  });
});
